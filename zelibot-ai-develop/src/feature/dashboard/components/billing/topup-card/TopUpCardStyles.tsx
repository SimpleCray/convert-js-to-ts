import { Box, styled, Card, Typography } from '@mui/material';

export const Plan = styled(Typography)(({ theme }) => ({
    fontSize: '20px',
    fontWeight: 800,
}));

export const TopUpCard = styled(Card)(({ theme }) => ({
    padding: 16,
}));

